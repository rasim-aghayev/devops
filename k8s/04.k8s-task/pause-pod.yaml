apiVersion: v1
kind: Pod
metadata:
  name: pause-pod
  namespace: limit
spec:
  containers:
    - name: pause
      image: registry.k8s.io/pause:2.0
      resources:
        requests:
          cpu: 700m
        limits:
          cpu: 700m




