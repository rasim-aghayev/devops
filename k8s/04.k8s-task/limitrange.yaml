apiVersion: v1
kind: LimitRange
metadata:
  name: cpu-resource-constraint
  namespace: limit
spec:
  limits:
    - default:
        memory: "500m"
      defaultRequest:
        memory: "500m"
      max:
        cpu: "1"
      min:
        cpu: "100m"
      type: Container



